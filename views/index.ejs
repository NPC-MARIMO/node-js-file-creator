<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full text-white min-h-screen bg-zinc-500">
        <div class="form p-10">
            <form action="/create" method="post" >
                <input name="title" class="block rounded-md outline-none w-full px-5 py-2 bg-zinc-800" type="text" placeholder="title goes here">
                <textarea name="details" class="block rounded-md outline-none w-full mt-2 px-5 py-2 resize-none bg-zinc-800" name="" placeholder="write your task details" id=""></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md" type="submit" value="create task">
            </form>
        </div>

        <div class="tasks flex flex-wrap gap-3 p-10">
            <% if(files.length > 0){ %>
                <% files.forEach((file) => { %>
                    <div class="task min-w-[281px] px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%= file %></h1>
                        <a href="/file/<%=file%>" class="text-blue-500 inline-block mt-3">Read</a>
                    </div>
                <% }) %>
            <% } else{ %>
                <h3 class="text-zinc-600">no tasks yet.</h3>
            <% } %>           
        </div>
    </div>
</body>
</html>